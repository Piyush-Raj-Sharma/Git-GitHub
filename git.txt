git init => version control starts

git add . => staged

git commit -m "message" => creates checkpoints

git branch -M main => branch switch     


git remote add origin repo URL

git push -u origin main



---------------------------------

git clone repoURL => brings the whole code from the repo
cd folder Name 

git pull => brings the added code from the repo to your local repo
